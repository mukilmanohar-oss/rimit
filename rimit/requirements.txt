# RIMIT B2B Aggregator - Python dependencies
# Backend: Django 5.x + DRF + drf-spectacular
# Database: PostgreSQL 16 (psycopg2) — SQLite for dev/tests
# Async: Celery 5.x + Redis
# Identity: Keycloak (OIDC via mozilla_django_oidc in prod)
# Storage: MinIO/S3 (boto3)
# Docs: drf-spectacular (OpenAPI 3.1)

# Core
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
drf-spectacular>=0.27,<1.0
django-filter>=23.0
django-cors-headers>=4.0
django-ratelimit>=4.0
python-dotenv>=1.0

# Async / Tasks
celery>=5.3,<6.0
redis>=5.0,<7.0
django-celery-beat>=2.6

# Database
psycopg2-binary>=2.9  # prod PostgreSQL
# SQLite3 is built into Python — no extra package needed for dev

# Storage
boto3>=1.34  # MinIO/S3 client

# Auth (prod)
# mozilla-django-oidc>=4.0  # Keycloak OIDC

# Notifications
# requests>=2.31  # for Meta Graph API + WhatsApp Cloud API

# Testing
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3
fakeredis>=2.20

# Code quality
flake8>=7.0
black>=24.0
isort>=5.13

# Lint / security
bandit>=1.7
safety>=3.0

# Production
gunicorn>=22.0
